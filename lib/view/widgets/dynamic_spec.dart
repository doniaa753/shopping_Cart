
import 'package:flutter/material.dart';
/*
class SizeConfig {
  static MediaQueryData? _mediaQueryData;
  static double? screenWidth;
  static double? screenHeight;
  static double? defaultSize;
  static Orientation? orientation;


  ///get screen height, weight and orientation
  void init({required BuildContext context}) {
    _mediaQueryData = MediaQuery.of(context);
    screenWidth = _mediaQueryData!.size.width;
    screenHeight = _mediaQueryData!.size.height;
    orientation = _mediaQueryData!.orientation;
  }
}

// Get the proportionate height per screen size
double getProportionateScreenHeight({required double inputHeight}) {
  double screenHeight = SizeConfig.screenHeight!;
  // 812 is the layout height that designer use
  // return (inputHeight / 812.0) * screenHeight;
  return (inputHeight / 812.0) * screenHeight;
}

// Get the proportionate height per screen size
double getProportionateScreenWidth({required double inputWidth}) {
  double screenWidth = SizeConfig.screenWidth!;
  // 375 is the layout width that designer use
  // return (inputWidth / 375.0) * screenWidth;
  return (inputWidth / 375.0) * screenWidth;
}

double getProportionateFontSize({required double fontSize}) {
  double screenWidth = SizeConfig.screenWidth!;
  // 375 is the layout width that designer use
  // return (fontSize / 375.0) * screenWidth;
  return (fontSize / 375.0) * screenWidth;
}
*/
